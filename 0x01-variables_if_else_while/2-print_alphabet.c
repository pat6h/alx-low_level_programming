#include <stdio.h>
/**
 * main - program that prints the alphabet in lowercase, followed by a new line.
 * return: 0 (success)
 */
int main(void) {
	char start = 'a';

	putchar(start)
		
	char ch;

	for (char = start +1; ch <= 'z' ; ch++) {
		puchar(ch);
	}
	putchar('\n')
	
		return (0);
}
